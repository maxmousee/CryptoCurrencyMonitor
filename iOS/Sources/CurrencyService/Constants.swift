// Constants for SWIFT lib
struct Constants {
    struct Defaults {
      //Add Default Constants here
        static let baseURL = "https://api.coinmarketcap.com/v1/ticker/"
        static let supportedCurrencies = ["bitcoin", "dogecoin"]
    }

    struct Messages {
      //Add default messages here
    }
}
